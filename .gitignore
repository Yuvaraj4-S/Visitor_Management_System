.DS_Store
*.pyc
*.egg-info
*.swp
tags
node_modules
__pycache__

# Ruff / tooling caches
.ruff_cache/

# AI-assistant working files. These hold this project's internal engineering
# notes — known-bug lists, security posture, agent instructions — and must
# never ship inside a customer install. They are untracked today; these entries
# stop a stray `git add .` from publishing them.
.claude/
CLAUDE.md
